{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "env": {},
            "args": [
                "serve",
                "-p=8000",
                "-k=localhost:9092",
                "-i=http://localhost:8086",
                "-n=interactws",
                "-u=interactws-admin",
                "-s=interactws-admin",
                "-l=debug",
                "-t=owner",
            ]
        }
    ]
}